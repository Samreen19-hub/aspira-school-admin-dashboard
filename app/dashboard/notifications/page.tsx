"use client"
import { Bell, CheckCheck, Calendar, FileText, Users } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { toast } from "sonner"
const items = [{ title: "New admission application received", detail: "A new application is ready for review.", time: "10 min ago", icon: Users, unread: true }, { title: "Parent-teacher meeting reminder", detail: "The meeting is scheduled for tomorrow at 10:00 AM.", time: "2 hours ago", icon: Calendar, unread: true }, { title: "Monthly report generated", detail: "Your May performance report is ready to download.", time: "Yesterday", icon: FileText, unread: false }]
export default function NotificationsPage() { return <div className="p-4 lg:p-6 max-w-3xl flex flex-col gap-5"><div className="flex items-start justify-between"><div><h1 className="text-xl font-bold text-foreground">Notifications</h1><p className="text-sm text-muted-foreground">Stay up to date with activity at your school.</p></div><Button variant="outline" size="sm" onClick={() => toast.success("All notifications marked as read")}><CheckCheck className="size-4" /> Mark all read</Button></div><div className="bg-card border border-border rounded-xl divide-y divide-border">{items.map(({ title, detail, time, icon: Icon, unread }) => <div key={title} className="p-4 flex items-start gap-3"><div className="size-9 rounded-lg bg-primary/10 flex items-center justify-center shrink-0"><Icon className="size-4 text-primary" /></div><div className="flex-1"><div className="flex items-center gap-2"><p className="text-sm font-medium text-foreground">{title}</p>{unread && <Badge variant="secondary" className="text-[10px]">New</Badge>}</div><p className="text-xs text-muted-foreground mt-1">{detail}</p><p className="text-[11px] text-muted-foreground mt-2">{time}</p></div><Bell className="size-4 text-muted-foreground" /></div>)}</div></div> }
